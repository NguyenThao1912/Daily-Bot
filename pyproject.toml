[project]
name = "daily-bot-jira"
version = "0.1.0"
description = "Standalone Jira reporting bot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp",
    "openpyxl",
    "python-dotenv",
]

[tool.uv]
package = false
